#!/bin/bash

if command -v gem >/dev/null 2>&1; then
  RBENV_ROOT=$HOME/.rbenv
  [ -d $RBENV_ROOT ] || RBENV_ROOT=/usr/local/rbenv
  $RBENV_ROOT/bin/rbenv exec gem install vvm-rb
  [ -d $HOME/.vim ] || mkdir $HOME/.vim
fi
