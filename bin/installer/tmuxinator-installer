#!/bin/bash

if command -v gem >/dev/null 2>&1; then
  gem install tmuxinator --no-ri --no-rdoc
  mv $HOME/.tmuxinator/* $HOME/dotfiles/.tmux/tmuxinator/
  rm -rf $HOME/.tmuxinator
  ln -s $HOME/dotfiles/.tmux/tmuxinator $HOME/.tmuxinator
  rbenv rehash
fi
