#!/bin/bash

ZSH_PATH=$HOME/.zsh
POWERLINE_PATH=$ZSH_PATH/powerline-shell

if [ ! -d $ZSH_PATH ]; then
  mkdir $ZSH_PATH
fi
git clone --single-branch https://github.com/b-ryan/powerline-shell.git $POWERLINE_PATH
cd $POWERLINE_PATH

python3 setup.py install
