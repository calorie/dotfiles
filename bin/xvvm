#!/bin/bash

version="vimorg--$2"
vvm $1 $version \
    --enable-multibyte \
    --with-features=huge \
    --enable-fontset \
    --disable-selinux \
    --enable-pythoninterp \
    --enable-rubyinterp \
    --enable-xim \
    --enable-luainterp \
    --with-lua-prefix=/usr
vvm use $version
exec $SHELL
